// Opertor
// +, -, ==, ===, /

// Array methods
let arr = ['olma', 'li', 'pop', 24, 23432, 23423, 'uzum'];

// push, pop, shift, slice, .length
// push -> element qo'shadi
// arr.shift(); -> birinchi elementni olib tashlaydi
// arr.pop(); -> oxi elementni olib tashlaydi
// arr.slice(startIndex, endIndex) - > index bo'ylab kesadi
// const slicedElemnt = arr.slice(0, 7);

// String methods

// toUpperCase(), toLowerCase(), .length()
// "" , '' , ``

let str = 'ASADBEK'; //

let strLength = str.length; // length of string
// console.log(str.toLowerCase());

// Object methods

let obj = {
	name: 'Asadbek',
	hobbies: ['football', 'tennnis'],
};

obj.hobbies.pop();

console.log(obj);

// Vazifalar

// 1) 3 ta o'zgaruvchi kalit so'ziga alohida nom va qiymat bering.

// 2) push, pop, shift, slice, .length misollar yozib kelamiz
// 3)  // toUpperCase(), toLowerCase(), .length()

// 4)  Array yaratamiz va buni 4 ta element to'ldiramiz ( .push()) // .length bn oxirgi elemnt oling.

// 5) Object methods: Bo'sh bolgan objni nom va quymat jufligi bilan to'ldiring.
